## What does this change?

## Why?

## How was it tested?

- [ ] `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace` pass
- [ ] Tried it with `just install` (dictation, panel applet and settings still work)
- [ ] Updated `CHANGELOG.md` if this is user-facing
